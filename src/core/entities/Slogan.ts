export class Slogan {
  id: string
  province: string
  slogan: string

  constructor(id: string, province: string, slogan: string) {
    this.id = id
    this.province = province
    this.slogan = slogan
  }
}
